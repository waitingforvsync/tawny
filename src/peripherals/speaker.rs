/// Speaker peripheral.
///
/// Takes sound output from the system VIA / SN76489 and produces audio samples
/// for the host audio API.
pub struct Speaker {
    // Audio buffer, sample state — to be filled in.
}

impl Speaker {
    pub fn new() -> Self {
        Self {}
    }
}
